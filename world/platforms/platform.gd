extends Node2D



func _ready():
	# Debug log to check initial values
	pass

func _physics_process(delta):
	# Debug log to track current angle and position
	#print("Physics Process: current_angle =", rad2deg(current_angle))
	
	# Update the current angle
	pass
